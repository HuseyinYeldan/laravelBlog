@props(['buttonText'])
<x-form.panel>
    <button type="submit" class="border border-gray-300 bg-blue-400 ring-2 ring-blue-200 text-white font-bold text-xl p-2 rounded duration-300 focus:border-blue-300 outline-none">{{ $buttonText }}</button>
</x-form.panel>
